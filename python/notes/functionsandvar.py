#->used for single line comment /functions are the verb or action that a computer is already programmed to perform
different functions
--print(argument to be printed it can take multiple argument ude , to pass multiple arguments) #and here , automatically adds space b/w argument when print does printing in commnd line

'''
is used for multiline comment
 print fuction prints the contents in the arguments in th ecommand line it also automatically uses
 newline /n char as a terminator for the arguments so it always prints a new line by default
 also print always returns a STRING.
'''
-- input ('prompt as the argument')               -- = sign assigns what is on the left to the right part
-- in python documentation *represents as many you want and [] represents optional
